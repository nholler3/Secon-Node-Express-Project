<div class="container">
    <div class="col-6">
        <h1 class="title">List of those who Signed Up!</h1>
        <h1>List of Our Users</h1>
        <% users.forEach(user => { %>
            <h5><%= user.name %></h5>
            <span><%= user.gender %></span>
        <% }); %>
    </div>
</div>